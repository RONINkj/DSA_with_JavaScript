// pointer is a reference to a memory location in the computer's memory 
// a variable that stores the memory address as its value
// a pointer references a location in memory and obtaining the value stored at that location is known as dereferencing the pointer
let num1=5;
let num2=num1;
num1=10;

// console.log(num1);
// console.log(num2);
let obj1={value:1}
let obj2=obj1
console.log(obj1);
obj1.value=11;  
console.log(obj1);
console.log(obj2);

